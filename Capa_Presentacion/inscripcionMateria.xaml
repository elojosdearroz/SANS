<Page x:Class="Capa_Presentacion.inscripcionMateria"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Capa_Presentacion"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="800"
      Title="Inscripción de Materias">

    <Page.Resources>
        <!-- Estilos ejecutivos sin gradientes -->
        <SolidColorBrush x:Key="BackgroundColor" Color="#f5f5f5"/>
        <SolidColorBrush x:Key="CardBackground" Color="White"/>
        <SolidColorBrush x:Key="PrimaryColor" Color="#2c3e50"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#34495e"/>
        <SolidColorBrush x:Key="AccentColor" Color="#3498db"/>
        <SolidColorBrush x:Key="TextColor" Color="#2c3e50"/>
        <SolidColorBrush x:Key="LightTextColor" Color="#7f8c8d"/>

        <!-- Estilo para botones principales -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource SecondaryColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para cards de materias -->
        <Style x:Key="MateriaCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderBrush" Value="#e0e0e0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.08" ShadowDepth="2" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="Black" Opacity="0.12" ShadowDepth="4" BlurRadius="12"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para checkboxes -->
        <Style x:Key="CustomCheckBox" TargetType="CheckBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="checkBorder" 
                                    Grid.Column="0"
                                    Width="18" Height="18" 
                                    CornerRadius="3" 
                                    BorderThickness="2" 
                                    BorderBrush="{StaticResource AccentColor}"
                                    Background="White"
                                    Margin="0,0,8,0">
                                <Path x:Name="checkMark" 
                                      Stroke="White" 
                                      StrokeThickness="2" 
                                      Data="M 3,6 L 6,9 L 13,2"
                                      Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Grid.Column="1" 
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding Content}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkBorder" Property="Background" Value="{StaticResource AccentColor}"/>
                                <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="checkBorder" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource BackgroundColor}">
        <ScrollViewer VerticalScrollBarVisibility="Auto" 
                      HorizontalScrollBarVisibility="Disabled"
                      Margin="20">
            <StackPanel MaxWidth="1000" HorizontalAlignment="Center">

                <!-- Header -->
                <Border Background="{StaticResource CardBackground}"
                        CornerRadius="8"
                        Margin="0,20,0,30"
                        Padding="30,25"
                        BorderBrush="#e0e0e0"
                        BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" Opacity="0.08" ShadowDepth="2" BlurRadius="8"/>
                    </Border.Effect>

                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Inscripción de Materias" 
                                   FontSize="24" 
                                   FontWeight="Bold" 
                                   HorizontalAlignment="Center"
                                   Foreground="{StaticResource PrimaryColor}"
                                   Margin="0,0,0,8"/>
                        <TextBlock Text="Seleccione las materias que desea cursar este semestre" 
                                   FontSize="14" 
                                   HorizontalAlignment="Center"
                                   Foreground="{StaticResource LightTextColor}"/>
                    </StackPanel>
                </Border>

                <!-- Grid de materias -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Programación III -->
                    <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource MateriaCard}">
                        <StackPanel>
                            <TextBlock Text="Programación III" 
                                       FontSize="16" 
                                       FontWeight="Bold" 
                                       Foreground="{StaticResource PrimaryColor}"
                                       HorizontalAlignment="Left"
                                       Margin="0,0,0,8"/>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                            </Grid>
                            <CheckBox x:Name="chkProgramacion3" 
                                      Content="Inscribir esta materia" 
                                      Style="{StaticResource CustomCheckBox}"/>
                        </StackPanel>
                    </Border>

                    <!-- Programación Móvil I -->
                    <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource MateriaCard}">
                        <StackPanel>
                            <TextBlock Text="Programación Móvil I" 
                                       FontSize="16" 
                                       FontWeight="Bold" 
                                       Foreground="{StaticResource PrimaryColor}"
                                       HorizontalAlignment="Left"
                                       Margin="0,0,0,8"/>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                            </Grid>
                            <CheckBox x:Name="chkProgramacionMovil" 
                                      Content="Inscribir esta materia" 
                                      Style="{StaticResource CustomCheckBox}"/>
                        </StackPanel>
                    </Border>

                    <!-- Programación Web II -->
                    <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource MateriaCard}">
                        <StackPanel>
                            <TextBlock Text="Programación Web II" 
                                       FontSize="16" 
                                       FontWeight="Bold" 
                                       Foreground="{StaticResource PrimaryColor}"
                                       HorizontalAlignment="Left"
                                       Margin="0,0,0,8"/>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                            </Grid>
                            <CheckBox x:Name="chkProgramacionWeb" 
                                      Content="Inscribir esta materia" 
                                      Style="{StaticResource CustomCheckBox}"/>
                        </StackPanel>
                    </Border>

                    <!-- IoT y Robótica -->
                    <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource MateriaCard}">
                        <StackPanel>
                            <TextBlock Text="IoT y Robótica" 
                                       FontSize="16" 
                                       FontWeight="Bold" 
                                       Foreground="{StaticResource PrimaryColor}"
                                       HorizontalAlignment="Left"
                                       Margin="0,0,0,8"/>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                            </Grid>
                            <CheckBox x:Name="chkIoTRobotica" 
                                      Content="Inscribir esta materia" 
                                      Style="{StaticResource CustomCheckBox}"/>
                        </StackPanel>
                    </Border>

                    <!-- Base de Datos II -->
                    <Border Grid.Row="2" Grid.Column="0" Style="{StaticResource MateriaCard}">
                        <StackPanel>
                            <TextBlock Text="Base de Datos II" 
                                       FontSize="16" 
                                       FontWeight="Bold" 
                                       Foreground="{StaticResource PrimaryColor}"
                                       HorizontalAlignment="Left"
                                       Margin="0,0,0,8"/>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                            </Grid>
                            <CheckBox x:Name="chkBaseDatos" 
                                      Content="Inscribir esta materia" 
                                      Style="{StaticResource CustomCheckBox}"/>
                        </StackPanel>
                    </Border>

                    <!-- Inglés Técnico III -->
                    <Border Grid.Row="2" Grid.Column="1" Style="{StaticResource MateriaCard}">
                        <StackPanel>
                            <TextBlock Text="Inglés Técnico III" 
                                       FontSize="16" 
                                       FontWeight="Bold" 
                                       Foreground="{StaticResource PrimaryColor}"
                                       HorizontalAlignment="Left"
                                       Margin="0,0,0,8"/>

                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                            </Grid>
                            <CheckBox x:Name="chkInglesTecnico" 
                                      Content="Inscribir esta materia" 
                                      Style="{StaticResource CustomCheckBox}"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Panel de acciones -->
                <Border Background="{StaticResource CardBackground}"
                        CornerRadius="8"
                        Margin="0,30,0,20"
                        Padding="30"
                        BorderBrush="#e0e0e0"
                        BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" Opacity="0.08" ShadowDepth="2" BlurRadius="8"/>
                    </Border.Effect>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Confirmar Inscripción" 
                                Style="{StaticResource PrimaryButton}"
                                Width="180"
                                x:Name="btnConfirmarInscripcion"
                                Margin="10"/>

                        <Button Content="Cancelar" 
                                Width="120"
                                Height="40"
                                FontSize="14"
                                FontWeight="SemiBold"
                                Background="Transparent"
                                Foreground="#e74c3c"
                                BorderBrush="#e74c3c"
                                BorderThickness="1"
                                Cursor="Hand"
                                x:Name="btnCancelar">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#e74c3c"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>